class Todo {
  String title;
  bool isDone;

  Todo({
    required this.title,
    this.isDone = false,
  });
}
// Model yang mendefinisikan struktur data Todo. 
// Setiap Todo memiliki judul (title) dan status apakah sudah selesai (isDone) dengan default false.
